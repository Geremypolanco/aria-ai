# Environment variables

# Existing vars ...

# Activepieces MCP (self-hosted integrations engine)
# Example: http://activepieces-host:8080  (include scheme, no trailing slash)
ACTIVEPIECES_MCP_URL=
# Token from the Activepieces MCP Servers settings (or blank for no auth)
ACTIVEPIECES_API_KEY=
