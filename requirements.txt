
# ── ARIA OS: Arquitectura Final (Organización Autónoma Digital) ──────────────
# NOTE: this file is NOT installed by the Dockerfile or CI — both install
# apps/core/requirements.txt (the actually-deployed, tightly-pinned set).
# This file is a broader, aspirational/exploratory dependency list for local
# experimentation (see README.md's `pip install -r requirements.txt`); none
# of these packages are currently imported under apps/, with the sole
# exception of sweagent, which is an optional soft-dependency in
# apps/core/agents/autonomous_coder.py (already gracefully degrades if
# missing). Versions below are pinned to the latest stable release as of
# 2026-07-27 to avoid an unpinned local install silently picking up a
# future breaking major version.
# Cognition & Optimization
pymc==5.28.5

# Economics & Analytics
duckdb==1.5.5
stripe==15.3.1
posthog==7.29.0

# Execution & Swarm
temporalio==1.30.0
prefect==3.8.0
crewai==1.15.7
pyautogen==0.10.0
langgraph==1.2.9
fastapi==0.140.0
uvicorn==0.51.0
pydantic==2.13.4
aiohttp==3.14.3
openai==2.48.0
# Phase 8 — Cognitive Infrastructure
qdrant-client==1.18.0
networkx==3.6.1
dspy==3.2.1
arize-phoenix==19.6.0
openinference-instrumentation-openai==0.1.52

# Perception & Internet
firecrawl-py==4.32.1
crawl4ai==0.9.2
playwright==1.61.0
httpx==0.28.1

# Memory & Graphs
neo4j==6.2.0
graphiti-core[falkordb]==0.29.2
mem0ai==2.0.14

# Governance & Self-Improvement
aider-chat==0.86.2
sweagent==0.0.1
langfuse==4.14.1
