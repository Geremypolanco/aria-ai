name: Setup Fly.io Secrets

  on:
    workflow_dispatch:
      inputs:
        confirm:
          description: 'Type "yes" to confirm setup'
          required: true

  jobs:
    setup-secrets:
      name: Configure all secrets on Fly.io
      runs-on: ubuntu-latest
      if: ${{ github.event.inputs.confirm == 'yes' }}
      steps:
        - name: Install flyctl
          uses: superfly/flyctl-actions/setup-flyctl@master

        - name: Set all secrets on Fly.io
          env:
            FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          run: |
            echo "Configuring Fly.io secrets for aria-ai..."
            flyctl secrets set \
              GROQ_API_KEY="${{ secrets.GROQ_API_KEY }}" \
              HF_API_KEY="${{ secrets.HF_API_KEY }}" \
              HUGGING_FACE_TOKEN="${{ secrets.HF_API_KEY }}" \
              OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
              GOOGLE_API_KEY="${{ secrets.GOOGLE_API_KEY }}" \
              ELEVENLABS_API_KEY="${{ secrets.ELEVENLABS_API_KEY }}" \
              TELEGRAM_TOKEN="${{ secrets.TELEGRAM_BOT_TOKEN }}" \
              TELEGRAM_CHAT_ID="${{ secrets.TELEGRAM_CHAT_ID }}" \
              SUPABASE_URL="${{ secrets.SUPABASE_URL }}" \
              SUPABASE_KEY="${{ secrets.SUPABASE_KEY }}" \
              UPSTASH_REDIS_REST_URL="${{ secrets.UPSTASH_REDIS_REST_URL }}" \
              UPSTASH_REDIS_REST_TOKEN="${{ secrets.UPSTASH_REDIS_REST_TOKEN }}" \
              GITHUB_TOKEN="${{ secrets.GH_PAT }}" \
              GITHUB_REPO="Geremypolanco/aria-ai" \
              STRIPE_SECRET_KEY="${{ secrets.STRIPE_SECRET_KEY }}" \
              PAYPAL_CLIENT_ID="${{ secrets.PAYPAL_CLIENT_ID }}" \
              PAYPAL_SECRET="${{ secrets.PAYPAL_SECRET }}" \
              GUMROAD_TOKEN="${{ secrets.GUMROAD_TOKEN }}" \
              SHOPIFY_URL="${{ secrets.SHOPIFY_URL }}" \
              SHOPIFY_API_KEY="${{ secrets.SHOPIFY_API_KEY }}" \
              SHOPIFY_AUTOMATION_TOKEN="${{ secrets.SHOPIFY_AUTOMATION_TOKEN }}" \
              PEXELS_API_KEY="${{ secrets.PEXELS_API_KEY }}" \
              NEWS_API_KEY="${{ secrets.NEWS_API_KEY }}" \
              SERP_API_KEY="${{ secrets.SERP_API_KEY }}" \
              MAILCHIMP_API_KEY="${{ secrets.MAILCHIMP_API_KEY }}" \
              MAILCHIMP_DC="${{ secrets.MAILCHIMP_DC }}" \
              BUFFER_TOKEN="${{ secrets.BUFFER_TOKEN }}" \
              AIRTABLE_TOKEN="${{ secrets.AIRTABLE_TOKEN }}" \
              CLOUDINARY_URL="${{ secrets.CLOUDINARY_URL }}" \
              CANVA_CLIENT_ID="${{ secrets.CANVA_CLIENT_ID }}" \
              CANVA_CLIENT_SECRET="${{ secrets.CANVA_CLIENT_SECRET }}" \
              SENTRY_DSN="${{ secrets.SENTRY_DSN }}" \
              ARIA_BASE_URL="https://aria-ai.fly.dev" \
              OWNER_EMAIL="geremypolancod@gmail.com" \
              CYCLE_INTERVAL_MINUTES="60" \
              ENVIRONMENT="production" \
              PORT="8000" \
              --app aria-ai
            echo "✅ All secrets configured on aria-ai!"

        - name: Verify app is running
          run: |
            sleep 10
            STATUS=$(curl -sf https://aria-ai.fly.dev/health | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('status','?'))" 2>/dev/null || echo "unreachable")
            echo "App health: $STATUS"
  